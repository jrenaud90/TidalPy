dist: xenial

language: python

os:
  - linux

python:
  - "3.8"

# command to install dependencies

install: python -m pip install git+https://github.com/geodynamics/burnman.git; pip install .

# command to run tests
script:
  - pytest