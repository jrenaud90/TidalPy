version: 2

build:
  os: "ubuntu-lts-latest"
  tools:
    python: "3.12"
  
  jobs:
    pre_build:
      - doxygen DoxyFile

sphinx:
  configuration: Documentation/conf.py

python:
  install:
    - method: pip
      path: .[docs]